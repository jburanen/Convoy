# Keep the build context small and secret-free.
.git
.gitignore
.claude
.claudeignore
.venv
venv
env
ENV
__pycache__
*.py[cod]
*.egg-info
.pytest_cache
.mypy_cache
.ruff_cache
htmlcov
.coverage*
dist
build
tests
# examples/ IS shipped: the image seeds /data/config.yaml from it on first
# run (docker-entrypoint.sh). Only *.example.* templates live there.
data
# Never ship env/secret material into an image layer.
.env
*.env
!.env.example
*.key
*.pem
